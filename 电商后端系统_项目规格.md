# 电商后端系统 - 技术学习项目

**项目定位**: 系统设计概念综合实践平台  
**业务载体**: 简化版电商系统  
**核心目标**: 实践后端开发完整路径 + 系统设计核心概念  
**技术栈**: Python + FastAPI + PostgreSQL + Redis + RabbitMQ  
**预计时长**: 3-4周达到完整版

---

## 🎯 学习目标（这是重点！）

### 技术能力目标
1. **完整的后端开发流程**
   - 从接收请求到返回响应的完整路径
   - 数据库设计、API设计、业务逻辑实现
   - 错误处理、日志、测试

2. **系统设计概念实践**
   - API Gateway（统一入口、认证、限流）
   - Message Queue（异步处理、解耦）
   - Cache（性能优化、缓存策略）
   - Load Balancing（可选，如果时间充裕）

3. **生产级代码质量**
   - 项目结构组织
   - 配置管理
   - 错误处理
   - 单元测试
   - 部署和容器化

---

## 📋 项目范围（简化但完整）

### 核心业务功能（保持简单）

**用户模块（User）**
- 注册/登录（JWT认证）
- 用户信息管理
- 地址管理

**商品模块（Product）**
- 商品CRUD（管理员）
- 商品列表/详情（用户）
- 分类管理
- 库存管理

**订单模块（Order）**
- 创建订单
- 订单状态管理（待支付、已支付、已发货、已完成）
- 订单历史查询
- **库存扣减**（并发控制重点）

**支付模块（Payment）**
- 模拟支付（不对接真实支付）
- 支付状态更新

**注意：** 业务逻辑保持最简单，不做：
- ❌ 购物车（太简单，直接下单即可）
- ❌ 优惠券（复杂度高，不是学习重点）
- ❌ 物流追踪（太细节）
- ❌ 评价系统（可选，时间充裕再加）

---

## 🏗️ 技术架构（渐进式演进）

### Phase 1: 单体应用（Week 1）

**目标**: 理解完整的request-response flow

```
Client Request
    ↓
FastAPI Application
    ↓
Business Logic
    ↓
PostgreSQL Database
    ↓
Response
```

**技术栈**:
- FastAPI（Web框架）
- SQLAlchemy（ORM）
- PostgreSQL（数据库）
- JWT（认证）
- Pydantic（数据验证）

**学习重点**:
- RESTful API设计
- 数据库schema设计
- ORM使用
- JWT认证流程
- 请求验证和错误处理

---

### Phase 2: 加入Redis Cache（Week 2前半）

**目标**: 理解缓存策略和性能优化

```
Client Request
    ↓
FastAPI Application
    ↓
Check Redis Cache ← 如果命中，直接返回
    ↓ (Cache Miss)
Business Logic
    ↓
PostgreSQL Database
    ↓
Write to Redis Cache
    ↓
Response
```

**缓存场景**:
1. **商品列表/详情**（读多写少）
2. **用户Session**（快速认证）
3. **热门商品**（访问频繁）
4. **库存数量**（配合并发控制）

**学习重点**:
- Cache-Aside模式
- Cache invalidation策略
- TTL设置
- 缓存穿透/击穿/雪崩的理解（可选实现）

---

### Phase 3: 加入Message Queue（Week 2后半 - Week 3前半）

**目标**: 理解异步处理和服务解耦

```
Client creates order
    ↓
FastAPI Application
    ↓
1. 写订单到数据库（同步）
2. 发消息到MQ（异步）
    ↓
返回"订单创建成功"
    ↓
Background Workers 从MQ消费消息:
  - Email Worker: 发送订单确认邮件
  - Inventory Worker: 更新库存
  - Notification Worker: 发送站内通知
```

**MQ使用场景**:
1. **订单创建后**:
   - 发送邮件通知
   - 更新库存
   - 发送推送通知
   
2. **支付成功后**:
   - 更新订单状态
   - 触发发货流程
   - 发送确认邮件

**技术选择**:
- **Celery + Redis** (推荐，Python生态常用)
- 或 **RabbitMQ + Pika** (更接近真实系统)

**学习重点**:
- Producer-Consumer模式
- 任务队列设计
- 消息确认机制
- 失败重试策略
- 幂等性设计

---

### Phase 4: API Gateway（Week 3后半）

**目标**: 理解统一入口和横切关注点

```
Client Request
    ↓
API Gateway (Nginx 或 自建)
  - 路由转发
  - 统一认证
  - Rate Limiting
  - 请求日志
    ↓
FastAPI Application(s)
    ↓
Redis / PostgreSQL
```

**Gateway功能**:
1. **路由管理**:
   - `/api/users/*` → User Service
   - `/api/products/*` → Product Service
   - `/api/orders/*` → Order Service

2. **统一认证**:
   - Gateway验证JWT token
   - 转发时添加user_id到header

3. **Rate Limiting**:
   - 每个IP每分钟最多100个请求
   - 使用Redis实现

4. **请求日志**:
   - 记录所有请求
   - 统计响应时间

**实现方案**:
- **方案A**: Nginx + Lua脚本（更接近生产环境）
- **方案B**: 用FastAPI自建Gateway（更容易理解和调试）

**学习重点**:
- 反向代理概念
- 限流算法（Token Bucket或Leaky Bucket）
- 统一认证的实现
- 日志聚合

---

### Phase 5: 微服务拆分（Week 4，可选）

**目标**: 理解微服务架构和服务通信

```
                API Gateway
                     ↓
      ┌──────────────┼──────────────┐
      ↓              ↓              ↓
User Service   Product Service  Order Service
      ↓              ↓              ↓
  User DB       Product DB       Order DB
      
服务间通信:
Order Service → User Service (验证用户)
Order Service → Product Service (检查库存)
```

**拆分策略**:
- 每个服务独立运行（独立进程/容器）
- 每个服务有独立的数据库schema（或独立DB）
- 服务间通过HTTP API通信

**学习重点**:
- 服务拆分原则
- 服务间通信（REST API）
- 数据一致性问题（分布式事务）
- 服务注册与发现（可选）

---

## 📊 数据库设计

### 核心表结构

```sql
-- 用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    username VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 用户地址表
CREATE TABLE addresses (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    recipient_name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    province VARCHAR(50) NOT NULL,
    city VARCHAR(50) NOT NULL,
    district VARCHAR(50) NOT NULL,
    detail_address TEXT NOT NULL,
    is_default BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 商品分类表
CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    parent_id INTEGER REFERENCES categories(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 商品表
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    description TEXT,
    price DECIMAL(10, 2) NOT NULL,
    stock INTEGER NOT NULL DEFAULT 0,
    category_id INTEGER REFERENCES categories(id),
    image_url VARCHAR(500),
    status VARCHAR(20) DEFAULT 'active', -- active, inactive
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 订单表
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    order_no VARCHAR(50) UNIQUE NOT NULL, -- 订单编号
    user_id INTEGER REFERENCES users(id),
    address_id INTEGER REFERENCES addresses(id),
    total_amount DECIMAL(10, 2) NOT NULL,
    status VARCHAR(20) NOT NULL, -- pending, paid, shipped, completed, cancelled
    payment_method VARCHAR(20), -- alipay, wechat, credit_card
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 订单商品表（订单和商品的多对多关系）
CREATE TABLE order_items (
    id SERIAL PRIMARY KEY,
    order_id INTEGER REFERENCES orders(id),
    product_id INTEGER REFERENCES products(id),
    quantity INTEGER NOT NULL,
    price DECIMAL(10, 2) NOT NULL, -- 下单时的价格（可能和商品表不同）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 支付记录表
CREATE TABLE payments (
    id SERIAL PRIMARY KEY,
    order_id INTEGER REFERENCES orders(id),
    amount DECIMAL(10, 2) NOT NULL,
    payment_method VARCHAR(20) NOT NULL,
    status VARCHAR(20) NOT NULL, -- pending, success, failed
    transaction_id VARCHAR(100), -- 第三方交易ID（模拟）
    paid_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 索引设计

```sql
-- 用户表索引
CREATE INDEX idx_users_email ON users(email);

-- 商品表索引
CREATE INDEX idx_products_category ON products(category_id);
CREATE INDEX idx_products_status ON products(status);
CREATE INDEX idx_products_created_at ON products(created_at DESC);

-- 订单表索引
CREATE INDEX idx_orders_user ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_created_at ON orders(created_at DESC);
CREATE INDEX idx_orders_order_no ON orders(order_no);

-- 订单商品表索引
CREATE INDEX idx_order_items_order ON order_items(order_id);
CREATE INDEX idx_order_items_product ON order_items(product_id);
```

---

## 🔑 关键技术实现点

### 1. 并发库存扣减（重点！）

**场景**: 多个用户同时购买同一商品，如何保证库存不超卖？

**方案对比**:

**❌ 错误方案（会超卖）**:
```python
# 查询库存
product = db.query(Product).filter_by(id=product_id).first()
if product.stock >= quantity:
    # Race condition在这里！
    product.stock -= quantity
    db.commit()
```

**✅ 方案1: 数据库层面加锁（简单）**:
```python
# 使用SELECT FOR UPDATE
product = db.query(Product)\
    .filter_by(id=product_id)\
    .with_for_update()\
    .first()

if product.stock >= quantity:
    product.stock -= quantity
    db.commit()
else:
    raise InsufficientStockError()
```

**✅ 方案2: 乐观锁（性能更好）**:
```sql
UPDATE products 
SET stock = stock - :quantity,
    version = version + 1
WHERE id = :product_id 
  AND stock >= :quantity
  AND version = :expected_version
RETURNING *;
```

**✅ 方案3: Redis分布式锁（最接近生产环境）**:
```python
# 使用Redis分布式锁
lock_key = f"product_lock:{product_id}"
with redis_lock(lock_key, timeout=10):
    # 先检查Redis缓存的库存
    stock = redis.get(f"stock:{product_id}")
    if int(stock) >= quantity:
        # 扣减Redis中的库存
        redis.decr(f"stock:{product_id}", quantity)
        # 异步更新数据库（通过MQ）
        send_to_mq("update_stock", {...})
    else:
        raise InsufficientStockError()
```

**你应该实现**: 
- Phase 1: 方案1（数据库锁）
- Phase 2加入Redis后: 方案3（分布式锁）

---

### 2. 缓存策略设计

**Cache-Aside Pattern（旁路缓存）**:

```python
async def get_product(product_id: int):
    # 1. 先查缓存
    cache_key = f"product:{product_id}"
    cached = await redis.get(cache_key)
    
    if cached:
        return json.loads(cached)
    
    # 2. 缓存未命中，查数据库
    product = db.query(Product).filter_by(id=product_id).first()
    
    if product:
        # 3. 写入缓存
        await redis.setex(
            cache_key,
            3600,  # TTL: 1小时
            json.dumps(product.to_dict())
        )
    
    return product

async def update_product(product_id: int, data: dict):
    # 1. 更新数据库
    product = db.query(Product).filter_by(id=product_id).first()
    product.update(data)
    db.commit()
    
    # 2. 删除缓存（而不是更新）
    cache_key = f"product:{product_id}"
    await redis.delete(cache_key)
```

**缓存失效策略**:
- **商品详情**: TTL 1小时，更新时删除
- **商品列表**: TTL 5分钟，更新任何商品时删除
- **用户Session**: TTL 24小时
- **库存数量**: TTL 1分钟（高频更新）

---

### 3. Message Queue异步任务

**任务定义（使用Celery）**:

```python
# tasks/order_tasks.py
from celery import Celery

celery_app = Celery('ecommerce', broker='redis://localhost:6379/0')

@celery_app.task
def send_order_confirmation_email(order_id: int):
    """发送订单确认邮件"""
    order = get_order(order_id)
    send_email(
        to=order.user.email,
        subject=f"订单确认 - {order.order_no}",
        body=render_template('order_confirmation.html', order=order)
    )

@celery_app.task
def update_inventory(product_id: int, quantity: int):
    """更新库存（从缓存同步到数据库）"""
    product = db.query(Product).filter_by(id=product_id).first()
    product.stock -= quantity
    db.commit()

@celery_app.task(bind=True, max_retries=3)
def process_payment(self, order_id: int):
    """处理支付（模拟第三方支付）"""
    try:
        # 模拟支付API调用
        payment_result = mock_payment_api(order_id)
        
        if payment_result.success:
            # 更新订单状态
            update_order_status(order_id, 'paid')
            # 触发后续任务
            send_order_confirmation_email.delay(order_id)
        else:
            update_order_status(order_id, 'payment_failed')
    except Exception as exc:
        # 失败重试
        raise self.retry(exc=exc, countdown=60)
```

**在API中触发任务**:

```python
@router.post("/orders", response_model=OrderResponse)
async def create_order(order_data: OrderCreate):
    # 1. 创建订单（同步）
    order = create_order_in_db(order_data)
    
    # 2. 触发异步任务（不等待完成）
    send_order_confirmation_email.delay(order.id)
    update_inventory.delay(order_data.product_id, order_data.quantity)
    
    # 3. 立即返回
    return order
```

---

### 4. API Gateway实现（自建方案）

```python
# gateway/main.py
from fastapi import FastAPI, Request, HTTPException
from fastapi.responses import JSONResponse
import httpx
import time

app = FastAPI(title="API Gateway")

# 服务路由配置
SERVICE_ROUTES = {
    "/api/users": "http://localhost:8001",
    "/api/products": "http://localhost:8002",
    "/api/orders": "http://localhost:8003",
}

# Rate Limiting (使用Redis)
async def check_rate_limit(client_ip: str) -> bool:
    key = f"rate_limit:{client_ip}"
    count = await redis.incr(key)
    if count == 1:
        await redis.expire(key, 60)  # 1分钟窗口
    return count <= 100  # 每分钟100次

@app.middleware("http")
async def gateway_middleware(request: Request, call_next):
    # 1. Rate Limiting
    client_ip = request.client.host
    if not await check_rate_limit(client_ip):
        return JSONResponse(
            status_code=429,
            content={"error": "Rate limit exceeded"}
        )
    
    # 2. 统一认证
    token = request.headers.get("Authorization")
    if token:
        user_id = verify_jwt_token(token)
        if not user_id:
            return JSONResponse(
                status_code=401,
                content={"error": "Invalid token"}
            )
        # 添加user_id到header，转发给下游服务
        request.state.user_id = user_id
    
    # 3. 路由转发
    path = request.url.path
    target_service = get_target_service(path)
    
    if not target_service:
        return await call_next(request)
    
    # 4. 转发请求
    async with httpx.AsyncClient() as client:
        target_url = f"{target_service}{path}"
        response = await client.request(
            method=request.method,
            url=target_url,
            headers=dict(request.headers),
            content=await request.body()
        )
    
    # 5. 返回响应
    return JSONResponse(
        status_code=response.status_code,
        content=response.json()
    )

def get_target_service(path: str) -> str:
    for prefix, service_url in SERVICE_ROUTES.items():
        if path.startswith(prefix):
            return service_url
    return None
```

---

## 📝 项目结构

```
ecommerce-backend/
├── docker-compose.yml          # Docker编排
├── requirements.txt            # Python依赖
├── .env.example               # 环境变量示例
├── README.md                  # 项目文档
│
├── gateway/                   # API Gateway (Phase 4)
│   ├── main.py
│   ├── rate_limiter.py
│   └── auth.py
│
├── services/
│   ├── user/                  # 用户服务 (Phase 5拆分)
│   │   ├── main.py
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── crud.py
│   │   └── routers.py
│   │
│   ├── product/               # 商品服务
│   │   └── ...
│   │
│   └── order/                 # 订单服务
│       └── ...
│
├── shared/                    # 共享代码
│   ├── database.py           # 数据库连接
│   ├── redis_client.py       # Redis客户端
│   ├── auth.py               # JWT认证
│   └── schemas.py            # 通用Schemas
│
├── tasks/                     # Celery任务 (Phase 3)
│   ├── __init__.py
│   ├── celery_app.py
│   ├── order_tasks.py
│   ├── email_tasks.py
│   └── inventory_tasks.py
│
├── tests/                     # 测试
│   ├── test_user.py
│   ├── test_product.py
│   ├── test_order.py
│   └── test_integration.py
│
└── scripts/                   # 工具脚本
    ├── init_db.sql           # 数据库初始化
    ├── seed_data.py          # 测试数据
    └── run_all.sh            # 启动所有服务
```

---

## 🚀 渐进式实现路径

### Week 1: 基础单体应用

**目标**: 实现基本的CRUD，理解完整的request flow

**Day 1-2: 项目搭建 + User模块**
- [ ] 搭建FastAPI项目结构
- [ ] 配置PostgreSQL连接
- [ ] 实现User注册/登录
- [ ] 实现JWT认证
- [ ] 测试: Postman测试所有user endpoints

**Day 3-4: Product模块**
- [ ] 实现Product CRUD
- [ ] 实现Category管理
- [ ] 实现商品列表/详情查询
- [ ] 测试: 商品操作流程

**Day 5-7: Order模块**
- [ ] 实现Order创建（带库存检查）
- [ ] 实现Order查询
- [ ] 实现简单的库存扣减（先不考虑并发）
- [ ] 测试: 完整的下单流程

**Week 1结束检查点**:
- ✅ 能完整走通：注册→登录→浏览商品→下单
- ✅ 数据库设计合理
- ✅ API响应格式统一
- ✅ 基本的错误处理

---

### Week 2前半: 加入Redis Cache

**Day 8-9: 缓存基础设施**
- [ ] 集成Redis客户端
- [ ] 实现Cache-Aside模式
- [ ] 缓存商品列表和详情
- [ ] 测试: 对比缓存前后的性能

**Day 10-11: 缓存策略优化**
- [ ] 实现缓存失效策略
- [ ] 缓存用户session
- [ ] 实现库存的Redis缓存
- [ ] 测试: 缓存一致性

**Week 2前半结束检查点**:
- ✅ Redis正常工作
- ✅ 商品查询性能提升明显
- ✅ 缓存更新策略正确

---

### Week 2后半 - Week 3前半: Message Queue

**Day 12-13: Celery集成**
- [ ] 配置Celery + Redis
- [ ] 实现第一个异步任务（发送邮件）
- [ ] 订单创建后触发邮件任务
- [ ] 测试: 任务能正常执行

**Day 14-15: 更多异步任务**
- [ ] 实现库存更新任务
- [ ] 实现支付处理任务（模拟）
- [ ] 实现任务重试机制
- [ ] 测试: 任务失败重试

**Week 3前半结束检查点**:
- ✅ MQ正常工作
- ✅ 异步任务能正常执行
- ✅ 失败重试机制工作

---

### Week 3后半: API Gateway

**Day 16-17: Gateway基础**
- [ ] 搭建Gateway服务
- [ ] 实现路由转发
- [ ] 实现统一认证
- [ ] 测试: 请求能正确路由

**Day 18-19: Gateway功能完善**
- [ ] 实现Rate Limiting
- [ ] 实现请求日志
- [ ] 实现错误处理
- [ ] 测试: 限流、日志功能

**Week 3结束检查点**:
- ✅ Gateway正常工作
- ✅ 认证和限流功能正常
- ✅ 所有请求通过Gateway

---

### Week 4: 微服务拆分（可选）

**Day 20-21: 服务拆分**
- [ ] 拆分User Service
- [ ] 拆分Product Service
- [ ] 拆分Order Service
- [ ] 配置独立的数据库连接

**Day 22-23: 服务通信**
- [ ] 实现服务间HTTP调用
- [ ] 处理服务调用失败
- [ ] 实现服务健康检查
- [ ] 测试: 端到端流程

**Day 24-25: Docker化**
- [ ] 编写Dockerfile
- [ ] 编写docker-compose.yml
- [ ] 一键启动所有服务
- [ ] 测试: 容器环境运行

---

## 🎯 每个Phase的输出标准

### Phase 1输出
- ✅ 能跑的API服务
- ✅ Postman collection完整测试
- ✅ README文档说明如何运行
- ✅ 基本的单元测试

### Phase 2输出
- ✅ Redis缓存正常工作
- ✅ 性能对比数据（缓存前vs后）
- ✅ 缓存策略文档

### Phase 3输出
- ✅ 异步任务正常执行
- ✅ Celery Flower监控界面
- ✅ 任务执行日志

### Phase 4输出
- ✅ Gateway服务运行
- ✅ 统一认证和限流工作
- ✅ 请求日志完整

### Phase 5输出（可选）
- ✅ 服务独立运行
- ✅ Docker Compose一键启动
- ✅ 服务间通信正常

---

## 🛠️ 技术栈详细说明

### 必须使用的
- **FastAPI**: Web框架（异步、高性能、类型提示）
- **SQLAlchemy**: ORM（数据库操作）
- **PostgreSQL**: 主数据库
- **Redis**: 缓存 + MQ Broker
- **Celery**: 异步任务队列
- **Pydantic**: 数据验证
- **JWT**: 认证

### 可选使用的
- **Alembic**: 数据库迁移
- **pytest**: 测试框架
- **Docker**: 容器化
- **Nginx**: 反向代理（如果不自建Gateway）

---

## 📚 学习资源

### 官方文档（必读）
- [FastAPI官方文档](https://fastapi.tiangolo.com/)
- [SQLAlchemy文档](https://docs.sqlalchemy.org/)
- [Celery文档](https://docs.celeryq.dev/)
- [Redis文档](https://redis.io/docs/)

### 推荐阅读（可选）
- 《Designing Data-Intensive Applications》（数据密集型应用设计）
- FastAPI的GitHub issues（学习best practices）

---

## ⚠️ 重要提醒

### 必须亲手写的部分
- ❌ **不允许AI生成全部代码**
- ✅ **可以用AI解释概念、debug错误**
- ✅ **核心业务逻辑必须自己写**

### 代码质量要求
- ✅ **清晰的代码结构**
- ✅ **合理的变量命名**
- ✅ **必要的注释**（解释"为什么"，不是"做什么"）
- ✅ **错误处理**（不能让程序随便崩溃）
- ✅ **日志记录**（方便调试）

### 测试要求
- ✅ **核心功能有单元测试**
- ✅ **手动测试过所有API**
- ✅ **有Postman collection或类似工具**

---

## 🎓 如何学习使用这个项目

### 每天的工作流程
1. **上午（3-4小时）**: 写代码
2. **中午**: 休息，回顾上午写的代码
3. **下午（2小时）**: 继续写代码或调试
4. **傍晚（1小时）**: 测试、写文档、总结

### 遇到问题时
1. **先自己尝试30分钟**
2. **查官方文档**
3. **Google错误信息**
4. **还不行就来问我**（带上错误信息和你尝试过的方案）

### 每周一次Code Review
- **把代码push到GitHub**
- **告诉我遇到的问题**
- **我会Review并指出改进点**

---

## 📝 下一步

你现在需要：
1. **确认这个项目spec**（有问题吗？）
2. **选择从Phase 1还是直接Phase 2开始**（建议Phase 1）
3. **准备开发环境**（Python、PostgreSQL、Redis）

确认后，我会给你：
- Week 1 Day 1的详细任务清单
- 项目搭建的step-by-step指导
- 第一个User模块的实现指导

**准备好了吗？** 🚀
